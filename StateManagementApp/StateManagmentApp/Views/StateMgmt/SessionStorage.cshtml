<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Session Storage</title>
</head>
<body>

    <h1>Session Storage</h1>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" class="form-control" id="email" name="email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" class="form-control" id="password" name="password">
            </div>
            <div class="form-group">
                <button class="btn btn-primary" onclick="storeInSessionStorage()">Hold My Data</button>
            </div>
        </div>
        <div class="col-md-8">
            <label id="lblEmail">Showing Email</label><br />
            <label id="lblPassword">Showing Password</label>
            <div class="form-group">
                <button class="btn btn-secondary" onclick="retrieveFromSessionStorage()">Retrieve from Session Storage</button>
            </div>
        </div>
    </div>

    <script>
        function storeInSessionStorage() {
            const email = document.getElementById("email").value;
            const password = document.getElementById("password").value;

            sessionStorage.setItem("email", email);
            sessionStorage.setItem("password", password);
        }

        function retrieveFromSessionStorage() {
            const email = sessionStorage.getItem("email") || "Email not found";
            const password = sessionStorage.getItem("password") || "Password not found";

            document.getElementById("lblEmail").textContent = email;
            document.getElementById("lblPassword").textContent = password;
        }
    </script>

</body>
</html>
